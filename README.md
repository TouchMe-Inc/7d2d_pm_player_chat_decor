# About plugin
Allows adding custom tags and colors to player names and chat messages.
The plugin reads player configuration from `players.ini` and decorates chat output with tags, nickname colors, and message colors.

# Features
- Supports per‑player configuration via players.ini.
- Adds custom tags before player names.
- Applies nickname color using hex codes.
- Applies message color using hex codes.
- Enables BBCode support for styled chat messages.